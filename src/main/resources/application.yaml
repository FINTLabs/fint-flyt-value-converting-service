fint:
  application-id: fint-flyt-value-converting-service

server:
  error:
    include-message: always

spring:
  profiles:
    include:
      - flyt-kafka
      - flyt-web-resource-server
      - flyt-postgres
      - flyt-logging
      - flyt-authorization-client

  output:
    ansi:
      enabled: always
