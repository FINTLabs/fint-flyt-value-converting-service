apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ra-no

resources:
  - ../../../base

labels:
  - pairs:
      app.kubernetes.io/instance: fint-flyt-value-converting-service_ra_no
      fintlabs.no/org-id: ra.no

patches:
  - patch: |-
      - op: replace
        path: "/spec/kafka/acls/0/topic"
        value: "ra-no.flyt.*"
      - op: replace
        path: "/spec/orgId"
        value: "ra.no"
      - op: replace
        path: "/spec/ingress/routes/0/path"
        value: "/ra-no/api/intern/value-convertings"
      - op: add
        path: "/spec/env/-"
        value:
          name: "novari.flyt.web-resource-server.security.api.internal.authorized-org-id-role-pairs-json"
          value: |
            {
              "ra.no":["USER"],
              "vigo.no":["DEVELOPER", "USER"],
              "novari.no":["DEVELOPER", "USER"]
            }
      - op: add
        path: "/spec/env/-"
        value:
         name: "novari.kafka.topic.orgId"
         value: "ra-no"
      - op: add
        path: "/spec/env/-"
        value:
         name: "server.servlet.context-path"
         value: "/ra-no"
      - op: replace
        path: "/spec/probes/startup/path"
        value: "/ra-no/actuator/health"
      - op: replace
        path: "/spec/probes/readiness/path"
        value: "/ra-no/actuator/health/readiness"
      - op: replace
        path: "/spec/probes/liveness/path"
        value: "/ra-no/actuator/health/liveness"
      - op: replace
        path: "/spec/observability/metrics/path"
        value: "/ra-no/actuator/prometheus"

    target:
      kind: Application
      name: fint-flyt-value-converting-service
